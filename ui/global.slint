export struct AvaSetting {
    name :string,
    current: string,
    tochange: string,
    selects: [string],
    doc: string,
}

export global AvaSettings {
    in-out property <[AvaSetting]> settings :[] ;
    in-out property <string> about ;
}
